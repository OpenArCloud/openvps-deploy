# Working notes / briefs — not for the public repo
directions/

# Scan data. Recordings are large and are personal data: StrayScanner captures
# are video and depth of real places, often someone's home or workplace.
# Never commit them to a public repo.
scans/
*.zip
*.mp4
*.r3d
StrayScanner*/
**/rgb.mp4
**/depth/
**/confidence/
odometry.csv
imu.csv
camera_matrix.csv

# Secrets & credentials — never commit
.env
.env.*
!.env.example
docker.env
*.pem
*.key
*.p12
*.pfx
credentials
aws-credentials*
.aws/
*secret*.json
*secrets*.json
!*secrets*.example.json

# Terraform/CFN local state & params that may carry ARNs or IDs
*.tfstate
*.tfstate.*
.terraform/
parameters.local.json
cfn-params.local.json

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
